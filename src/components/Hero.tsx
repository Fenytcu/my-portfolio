"use client";

import HeroSection from "./sections/HeroSection";

// This file is kept as an entry point for the Hero component, 
// though sections are now split into separate files in ./sections/
const Hero = () => {
  return <HeroSection />;
};

export default Hero;
